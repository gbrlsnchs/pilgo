$ cp pilgrim.yml
$ plg check
.
└── test <- /tmp/test (ERROR)

$ plg check -fail --> FAIL
plg: there are one or more conflicts
	- linker: ${ROOTDIR}/test: target doesn't exist

$ cp test
$ plg check
.
└── test <- /tmp/test (READY)
